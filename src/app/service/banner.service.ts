import { Injectable } from '@angular/core';
import { Banner } from '../home/banner/banner.model';

@Injectable({
  providedIn: 'root'
})
export class BannerService {
  getOfferDetails(): Banner {
    new Banner();
    offer.
    return offer;
  }

  constructor() { }
}
